import 'package:flutter/material.dart';

class AssetScreenHeader extends StatelessWidget {
  const AssetScreenHeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
