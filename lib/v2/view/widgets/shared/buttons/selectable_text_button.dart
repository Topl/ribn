import 'package:flutter/material.dart';

class SelectableTextButton extends StatelessWidget {
  const SelectableTextButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
